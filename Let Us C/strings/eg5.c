#include <stdio.h>
#include<string.h>
int main()
{
    char str1[]="jitendra";
    int len1, len2;
    len1=strlen(str1);
    len2=strlen("humpty sharma ki dulhaniya");
    printf("%d %d", len1, len2);
    return 0;
}